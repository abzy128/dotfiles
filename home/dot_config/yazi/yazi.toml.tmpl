[mgr]
show_hidden = true

{{ if (includeTemplate "machine.json" . | fromJson).wayland }}
[[mgr.prepend_keymap]]
on  = "<C-y>"
run = [ 'shell -- for path in %s; do echo "file://$path"; done | wl-copy -t text/uri-list', "yank" ]

{{ end }}
